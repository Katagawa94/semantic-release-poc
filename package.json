{
  "name": "semantic-release-poc",
  "version": "1.0.0",
  "description": "A simple TypeScript npm package with a Foobar tagged template string",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "typescript",
    "template-string",
    "foobar"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.5.0",
    "typescript": "^5.9.2"
  }
}
